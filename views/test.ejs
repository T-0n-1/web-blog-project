<%- include("partials/header.ejs") %>
<%- include("partials/navi.ejs") %>

<div class="container">
  <h1>Search Results</h1>
  <% if (results.hitsCount === 0) { %>
    <p>No results found.</p>
  <% } else { %>
      <div>
        <p><%= results %></p>
      </div>
  <% } %>
</div>

<%- include("partials/footer.ejs") %>
